# Taym95.github.io

GitHub Pages source for a Jekyll blog.

## Local preview

```sh
bundle install
bundle exec jekyll serve
```

If you already have Jekyll installed globally, this also works:

```sh
jekyll serve
```
